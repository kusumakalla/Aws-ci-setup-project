version: 0.2

env:
  parameter-store:
    DOCKER_REGISTRY_USERNAME: /sample-app/docker-credentials/username
    DOCKER_REGISTRY_PASSWORD: /sample-app/docker-credentials/password
    DOCKER_REGISTRY_URL: /sample-app/docker-credentials/docer-registry-url
phases:
  install:
    runtime-versions:
      python: 3.11
  pre_build:
    commands:
      - echo "Installing dependencies..."
      - pip install -r requirements.txt
  build:
    commands:
      - echo "Running tests..."
      - echo "Building Docker image..."
      - echo "$DOCKER_REGISTRY_PASSWORD" | docker login -u "$DOCKER_REGISTRY_USERNAME" --password-stdin "$DOCKER_REGISTRY_URL"
      - docker build -t "$DOCKER_REGISTRY_URL/$DOCKER_REGISTRY_USERNAME/sample-python-flask-app:latest" .
      - docker push "$DOCKER_REGISTRY_URL/$DOCKER_REGISTRY_USERNAME/sample-python-flask-app:latest"
  post_build:
    commands:
      - echo "Build completed successfully!"
      - echo "Deploying...."
      - aws ssm send-command --instance-ids i-04724f96b1c8359cd --document-name AWS-RunShellScript --parameters commands="/home/ubuntu/deploy/deploy.sh"


